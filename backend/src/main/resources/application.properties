spring.application.name=FindTheOne

# Active Profile
spring.profiles.active=aws

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3307/findthatone?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8091
server.address=0.0.0.0

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://192.168.1.230:3000,http://192.168.1.230:3001
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# JWT Configuration
app.jwt.secret=findtheone-secret-key-for-jwt-token-generation-should-be-at-least-256-bits
app.jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Static Resources Configuration
spring.web.resources.static-locations=classpath:/static/,file:uploads/

# WebSocket Configuration
spring.websocket.allowed-origins=http://localhost:3000,http://localhost:3001,http://192.168.1.230:3000,http://192.168.1.230:3001

# Email Configuration (SMTP - Mailtrap for testing)
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=4289976f726845
spring.mail.password=29bcc77310bf5d
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=false
spring.mail.properties.mail.smtp.ssl.trust=sandbox.smtp.mailtrap.io

# Alternative Email Configuration (for other providers)
# spring.mail.host=smtp.mailgun.org
# spring.mail.port=587
# spring.mail.host=smtp.sendgrid.net
# spring.mail.port=587

# Application Configuration
app.frontend.url=${FRONTEND_URL:http://localhost:3001}
